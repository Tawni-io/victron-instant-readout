// Optional bring-up fallback only (gitignored). Prefer SoftAP → VictronDash.
// Copy to include/victron_secrets.h if you need USB-serial bring-up without SoftAP.
// Never commit real keys. Leave defines commented out for normal SoftAP use.
//
// From VictronConnect → Product info → Instant readout details → SHOW

#pragma once

// 32 hex chars, no spaces (case-insensitive)
// #define VICTRON_IR_KEY_HEX "0123456789abcdef0123456789abcdef"

// Optional: only report this MAC (with or without colons)
// #define VICTRON_IR_MAC "aabbccddeeff"
